# API Configuration
# Để truy cập từ điện thoại, thay localhost bằng địa chỉ IP của máy tính
# Ví dụ: VITE_API_URL=http://192.168.1.100:5000/api

# Local (chỉ truy cập trên máy tính)
VITE_API_URL=http://localhost:5000/api

# Network (truy cập từ điện thoại - thay YOUR_IP bằng IP thực tế)
# VITE_API_URL=http://YOUR_IP:5000/api

# Để lấy IP của máy:
# - Windows: chạy lệnh "ipconfig" trong PowerShell
# - Tìm "IPv4 Address" của WiFi adapter (thường là 192.168.x.x)
